name: Auto Release on Version Bump

# When the workspace version in Cargo.toml increases on main, tag it
# (v-prefixed semver, e.g. v0.2.0) and run the release pipeline. A manually
# pushed tag triggers release.yml directly instead and is unaffected by this
# workflow.
on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  detect:
    name: Detect version bump
    runs-on: ubuntu-latest
    outputs:
      release: ${{ steps.check.outputs.release }}
      tag: ${{ steps.check.outputs.tag }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - id: check
        shell: bash
        run: |
          version=$(cargo metadata --no-deps --format-version=1 \
            | jq -r '.packages[] | select(.name=="ffxi-client") | .version')
          tag="v${version}"
          echo "Workspace version: ${version} (tag ${tag})"
          if git rev-parse "refs/tags/${tag}" >/dev/null 2>&1; then
            echo "Tag ${tag} already exists — no release."
            echo "release=false" >> "$GITHUB_OUTPUT"
          else
            git config user.name  "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git tag "${tag}"
            git push origin "${tag}"
            echo "Created and pushed tag ${tag}."
            echo "release=true" >> "$GITHUB_OUTPUT"
            echo "tag=${tag}" >> "$GITHUB_OUTPUT"
          fi

  # A tag pushed by GITHUB_TOKEN does NOT re-trigger release.yml's push event,
  # so we invoke the reusable workflow directly to perform the release.
  release:
    name: Release
    needs: detect
    if: needs.detect.outputs.release == 'true'
    uses: ./.github/workflows/release.yml
    with:
      tag: ${{ needs.detect.outputs.tag }}
    permissions:
      contents: write
